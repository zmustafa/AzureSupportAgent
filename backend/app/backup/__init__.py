"""Whole-tenant Backup & Restore.

Exports a tenant's configuration + operational data as a single portable, secret-free
JSON archive and restores it on the same (or a rebuilt) instance. See ``registry`` for
the manifest format and the section registry.
"""
