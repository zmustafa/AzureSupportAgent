"""Azure Workloads: hand-picked Azure scopes (a customer product/application).

A *workload* is a named set of Azure scope nodes — management groups, subscriptions,
resource groups, or individual resources — bound to one Azure connection (tenant). It is
used as an optional query scope for chats (and, later, as a target for assessments).
"""
