"""Azure resource inventory: a unified, read-only view of every resource across the
defined Azure Workloads (or the whole tenant), with workload attribution, facets for
filtering, server-side caching, and AI natural-language search.

All Azure access is read-only (Azure Resource Graph). Mirrors the policy subsystem's
collector + cache patterns.
"""
